# Tier 5 adjudicator (SPEC §8, §13)
GROQ_API_KEY=
GROQ_MODEL=llama-3.1-8b-instant
TIER5_ENABLED=true

# Failure policy (SPEC §2): pre_booking fails CLOSED, post_booking fails OPEN
FAIL_MODE_PREBOOKING=closed

# Hot-tunable weights, bands and policy knobs
THRESHOLDS_PATH=config/thresholds.json
